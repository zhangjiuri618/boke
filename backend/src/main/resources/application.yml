server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wlkz_blog?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  secret: wlkz_tech_blog_jwt_secret_key_2026
  expiration: 86400000

logging:
  level:
    com.wlkz.blog: DEBUG
    org.springframework.security: INFO
